# Frontend environment.
# Leave EMPTY in local dev — requests go to "/api" and Vite proxies to localhost:5000.
# In production (Vercel), set this to your Render backend origin (no trailing slash):
#   VITE_API_URL=https://edii-lms-api.onrender.com
VITE_API_URL=
